module.exports = {
    semi: true, // Enforce semicolons at the end of statements
    singleQuote: true, // Use single quotes for strings
    tabWidth: 4, // Set the tab width to 4 spaces
    printWidth: 100, // Specify a maximum line width of 100 characters
};
